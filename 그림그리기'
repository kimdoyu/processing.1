float dx;
float dy;
float d;

void setup(){
  size(600, 400);
  background(51);
}

void draw(){
  dx = mouseX;
  dy = mouseY;
  d = sqrt(dx*dx + dy*dy);
  fill(0);
  strokeWeight(9);
  point(dx, dy);
  println(d);
}
